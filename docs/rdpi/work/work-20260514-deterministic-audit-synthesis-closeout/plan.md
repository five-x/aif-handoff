# Plan: Deterministic Audit Synthesis Closeout

- [ ] Extend `packages/shared/src/planQuality.ts` only with dependency-free optional synthesis source artifact context and validation helpers; keep marker-only source audit plans rejected.
- [ ] Update `packages/agent/src/subagents/planChecker.ts` or an agent-side helper to assemble the registry-derived deterministic synthesis fallback from roadmap batch artifacts, then pass exact source report paths/status context into shared plan-quality validation.
- [ ] Update `packages/data/src/index.ts` so synthesis readiness/listing includes terminal `invalid`/rejected, `missing`, `source_inconclusive`, `terminal_inconclusive`, and `manual_exception` source artifacts only when their state/attempt status is terminal, while preserving trusted valid counting and external-blocker behavior.
- [ ] Update `packages/agent/src/coordinator.ts` so synthesis plan-quality retry exhaustion persists a corrected registry-derived exact-source plan, clears stale plan-quality blocker fields/retry counters as appropriate, preserves useful diagnostics in activity/validation details, and routes the card to deterministic synthesis instead of generic `blocked_external`; preserve true external blockers.
- [ ] Update `packages/agent/src/subagents/implementer.ts` so deterministic synthesis runs for first-run synthesis implementation when roadmap synthesis inputs are available, not only on explicit rework; preserve all child states in the status table and classify all-untrusted or missing-source inputs as `audit inconclusive`.
- [ ] Add focused shared plan-quality tests proving marker-only source audit plans still fail, exact synthesis source report artifact plans pass, and deterministic fallback content includes the exact source report paths plus expected child-status/trust fields.
- [ ] Add focused data tests proving terminal rejected/invalid, missing, and `source_inconclusive` report artifacts are synthesis inputs, non-terminal invalid artifacts remain excluded, and `external_blocked` continues to block synthesis readiness.
- [ ] Add focused agent plan-checker/coordinator tests for empty existing synthesis artifact recovery and plan-quality retry exhaustion on synthesis, asserting recovery produces a non-empty final synthesis artifact with a child status table through the deterministic synthesis path and never only marks registry state.
- [ ] Add focused implementer/completion tests for all source reports `source_inconclusive`, mixed `valid`/rejected/`missing`/`source_inconclusive` inputs, missing physical report files with present terminal batch artifacts, and final summaries refusing to claim `validated_no_findings` unless every required source report is trusted and substantive.
- [ ] Add a mixed-source implementer regression where one trusted valid source report has a concrete finding with evidence, risk, proposed fix, and verification while other source reports are weak or missing; verify the final synthesis carries that finding forward without downgrading or dropping its evidence.
- [ ] Run focused package tests for the changed modules.
- [ ] Run repository lint/build/test as feasible after focused tests.
- [ ] Record implementation, test results, and gate outcomes in `result.md`, then run memory sync in auto mode after `TEST PASS` and `REVIEW PASS`.
