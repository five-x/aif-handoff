<!-- Managed by codex-platform; edit source GPTI/templates and re-run the compiler. -->

# Research

## Task framing and lane

- State the task, queue selection inputs, and lane when the repository uses lanes.

## Accepted planning sources

- Freeze the task card, governing local docs, and any explicitly accepted planning sources.
- Do not record runtime-visible evidence before `PLAN PASS`.

## Same-project memory

- If memory may be needed later, record only why it could matter.
- Do not query or summarize same-project memory before `PLAN PASS` unless the user explicitly waives that boundary.

## Cross-project reusable patterns

- Record only reusable planning patterns already available from local instructions.
- Do not query cross-project memory before `PLAN PASS` unless the user explicitly waives that boundary.

## Rejected or stale memory candidates

- Record candidates to validate later; do not mark anything stale from live evidence before the plan gate.

## Open questions

- Replace with unresolved questions.

## Hypotheses

- Replace with explicit hypotheses that still need validation.
