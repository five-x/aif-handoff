# Research

## Task framing and lane

- Task: `work-20260523-audit-synthesis-trust-propagation-review`.
- Lane: `work`.
- Intake card: `docs/intake/work/work-20260523-audit-synthesis-trust-propagation-review.md`.
- RDPI path: `docs/rdpi/work/work-20260523-audit-synthesis-trust-propagation-review`.
- Request: review downstream trust propagation paths and prove that shallow or `source_inconclusive` source reports cannot be promoted back into trusted no-findings through synthesis, task completion evidence, roadmap artifact counts, workflow timeline projections, deterministic repair, or review handoff.
- Diagnostic-only scope: production code must not be changed. Any confirmed promotion path must be queued as a separate implementation task, not fixed in this run.

## Accepted planning sources

- `AGENTS.md` requires local repo facts before memory, RDPI gates, no runtime-visible evidence before `PLAN PASS`, independent plan/test/review gates, and memory sync before close-out.
- `.agents/skills/runtask/SKILL.md` requires `codex-ensure-rdpi.py`, `codex-flow-audit.py --repo .`, RDPI for `RDPI Needed: yes`, diagnostic-only handling for audit/review/discovery tasks, and updating only the selected intake status after successful close-out.
- `.agents/skills/rdpi/SKILL.md` requires research, design, plan, independent `PLAN PASS`, execution, independent `TEST PASS`, independent `REVIEW PASS`, and `$memsync MODE=auto` after `result.md`.
- `python "$env:USERPROFILE\.codex\tools\codex-ensure-rdpi.py"` reported `STATUS: ready`.
- `python "$env:USERPROFILE\.codex\tools\codex-flow-audit.py" --repo .` reported `STATUS: clean`.
- The worktree already contains unrelated dirty and untracked intake, RDPI, memory, source, and test files. This task must preserve them and touch only this task's RDPI artifacts, memory artifacts generated by memsync, selected intake status entry, and any queued follow-up card if a promotion path is confirmed.
- Planning source map from local static reads and the independent explorer:
  - `packages/shared/src/auditSynthesisClassifier.ts` owns source-report synthesis classification, synthesis outcome metadata parsing, visible synthesis classification, and source/visible outcome combination.
  - `packages/shared/src/taskCompletionEvidence.ts` owns risky task completion evidence, audit synthesis completion blocking, deterministic fallback report blocking, and manual review propagation.
  - `packages/shared/src/auditReportValidator.ts` owns `evidenceDepth`, including `trustedNoFindingsSupported`, and downgrades unsupported no-findings to `source_inconclusive`.
  - `packages/data/src/index.ts` owns roadmap artifact state/count summaries, trusted source classification checks, artifact trust rollups, workflow timeline projections, and synthesis input selectors.
  - `packages/agent/src/coordinator.ts` owns review-handoff and completion evidence transitions, source-inconclusive terminalization, and synthesis task routing.
  - `packages/agent/src/subagents/implementer.ts` owns deterministic audit report repair and deterministic audit synthesis artifact writing.
  - `packages/agent/src/subagents/reviewer.ts` owns deterministic review comments for trusted/inconclusive audit artifacts.
  - `packages/agent/src/reviewGate.ts` owns deterministic auto-review decisions and completion-evidence-backed approval blocking.
- Local docs establish existing contracts:
  - `docs/kb/audit-evidence-provenance-contract.md` states that stale, missing, contradictory, inventory-only, or insufficient source evidence fails closed to `source_inconclusive` or another untrusted state, and cannot produce synthesis-ready trusted no-findings.
  - `docs/rdpi/work/work-20260522-harden-audit-evidence-depth-gates/result.md` claims `evidenceDepth.trustedNoFindingsSupported === true` is required before valid no-findings artifacts are trusted or synthesis-ready.
  - `docs/rdpi/work/work-20260523-adversarial-audit-evidence-depth-bypass-review/result.md` records an existing command-output-depth bypass and queued hardening follow-up. This task should distinguish that upstream source-depth bypass from downstream promotion paths.

## Same-project memory

- Local curated memory files were searched after local source/docs. Relevant local memory candidates echo existing contracts:
  - `docs/memory/decisions/decision-0830f252565dc52a.md`: synthesis `source_inconclusive` or legacy inconclusive output is not successful task completion.
  - `docs/memory/decisions/decision-b615fe90af0495c9.md`: `source_inconclusive` remains terminal non-trusted audit source, not trusted valid.
  - `docs/memory/decisions/decision-b93d63adf2a4bb09.md`: shared audit-card decision construction should keep inconclusive synthesis as `finalStatus: audit_inconclusive`.
- Shared-memory MCP recall was not used before `PLAN PASS` because this is repo-specific work and the RDPI planning boundary forbids shared-memory recall before plan review unless explicitly waived.

## Cross-project reusable patterns

- None accepted before `PLAN PASS`. Local repo contracts are sufficient for planning this diagnostic task.

## Rejected or stale memory candidates

- Older RDPI artifacts conflict on whether terminal source-inconclusive report tasks should be `done` or `blocked_external`. This task must not resolve that lifecycle question except where it affects downstream trusted no-findings propagation.
- Prior hardening close-outs are treated as claims to verify, not proof.

## Open questions

- Does the deterministic synthesis writer pass weak or terminal-inconclusive source artifacts to `classifyAuditSynthesisSourceReports()` in a way that preserves weak counts and prevents all-source trusted no-findings?
- Can any persisted roadmap artifact have `state === "valid"` while nested validation details remain stale, missing `evidenceDepth`, or classify as `source_inconclusive`, and then still count as trusted?
- Can terminal `source_inconclusive` synthesis review comments that say no blocking findings be converted into task completion despite completion evidence reporting `audit_inconclusive`?
- Do workflow timeline and API trust rollups consistently prefer artifact trust fields over generic task status when roadmap artifacts exist?
- Can deterministic repair or review handoff inherit stale trusted metadata from an earlier valid artifact after a later source-inconclusive validation result?

## Hypotheses

- H1: Synthesis source classification should fail closed because source reports are revalidated and `validated_no_findings` requires every source report to be substantive with `evidenceDepth.trustedNoFindingsSupported`.
- H2: Synthesis output parsing should fail closed because missing, invalid, contradictory, legacy inconclusive, or source/visible-disagreeing metadata maps to `source_inconclusive`.
- H3: Task completion evidence should fail closed because risky audit synthesis tasks with `source_inconclusive` or inconclusive batch evidence receive `audit_inconclusive` and cannot be accepted as successful completion.
- H4: Roadmap artifact counts and synthesis readiness should fail closed because trusted source classification for `validated_no_findings` requires a valid manifest and `evidenceDepth.trustedNoFindingsSupported === true`.
- H5: Workflow timeline and artifact trust rollups should fail closed because source-inconclusive artifacts map to untrusted or weak states and carry `audit_inconclusive` decisions.
- H6: Deterministic repair and review handoff should fail closed because strict repair either produces trusted validation or terminalizes as `source_inconclusive`, and review-gate completion evidence blocks inconclusive synthesis approval.
