<!-- Managed by codex-platform; edit source GPTI/templates and re-run the compiler. -->

# Result

## Outcome summary

- Summarize what changed and what was verified.

## Gate verdicts

- Plan review: Replace with `PLAN PASS` or `PLAN FAIL`.
- Test gate: Replace with `TEST PASS` or `TEST FAIL`.
- Final review: Replace with `REVIEW PASS` or `REVIEW FAIL`.
- User waivers: Replace with any explicit gate waiver, or `none`.

## Stable facts

- Add confirmed facts worth remembering.

## Reusable patterns

- Add reusable runbook-like steps only when they are stable.
