<!-- Managed by codex-platform; edit source GPTI/templates and re-run the compiler. -->

# RDPI Artifacts

Each non-trivial task lives under `docs/rdpi/<lane>/<task-id>/` with `research.md`, `design.md`, `plan.md`, and `result.md`.

- Use `docs/rdpi/personal/` for personal tasks.
- Use `docs/rdpi/work/` for work tasks.
